import React from 'react';

// This component is deprecated. 
// OTPs are sent to the user's actual email address via the backend.
const MockEmailClient = () => {
    return null;
};

export default MockEmailClient;
